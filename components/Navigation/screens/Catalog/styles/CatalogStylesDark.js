const { StyleSheet } = require("react-native");

export const dark = StyleSheet.create({
  catalogWrapper: {
    backgroundColor: 'black',
    paddingTop: 20,
    width: '100%',
    height: '100%',
  },
});